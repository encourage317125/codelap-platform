{
  "name": "Codelab GraphQL Schema",
  "schemaPath": "schema.graphql",
  "extensions": {
    "endpoints": {
      "Hasura GraphQL Endpoint": {
        "url": "https://codelab-ai.hasura.app/v1/graphql",
        "headers": {
          "user-agent": "JS GraphQL",
          "x-hasura-admin-secret": "${env:HASURA_GRAPHQL_ADMIN_SECRET}"
        },
        "introspect": true
      }
    }
  }
}
