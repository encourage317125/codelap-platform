query TestGetElement($input: GetElementInput!) {
  getElement(input: $input) {
    id
    name
    css
    props
    atom {
      id
      name
      type
    }
    hooks {
      id
      type
      config {
        ... on QueryHookConfig {
          __typename
          body
          lambdaId
          method
          queryKey
          url
        }
      }
    }
  }
}
