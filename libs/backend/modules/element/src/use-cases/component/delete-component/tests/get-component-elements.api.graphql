query TestGetComponentElements($input: GetComponentInput!) {
  getComponentElements(input: $input) {
    edges {
      order
      source
      target
    }
    vertices {
      ... on Component {
        id
        name
      }
      ... on Element {
        __typename
        id
        name
        css
        props
      }
    }
  }
}
