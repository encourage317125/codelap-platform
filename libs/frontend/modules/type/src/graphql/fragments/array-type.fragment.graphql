fragment ArrayTypeWithItemType on ArrayType {
  ...ArrayType
  itemType {
    ... on TypeBase {
      ...TypeBase
    }
  }
}

# Array type without the whole item type
fragment ArrayType on ArrayType {
  ...TypeBase
  itemType {
    id
  }
}
