fragment App on App {
  id
  owner {
    id
  }
  name
  pages {
    id
    name
    rootElement {
      id
      name
    }
  }
}

fragment AppBase on App {
  id
  owner {
    id
  }
  name
  # pages {
  #   id
  #   name
  # }
}
