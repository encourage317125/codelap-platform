fragment App on App {
  id
  # ownerId
  name
  pages {
    id
    name
    rootElement {
      id
      name
    }
  }
}

fragment AppBase on App {
  id
  # ownerId
  name
  # pages {
  #   id
  #   name
  # }
}
