export const deleteStoreInput = {
  state: { where: {} },
  stateApi: { where: {} },
  actions: {
    CustomAction: [{ where: {} }],
    PipelineAction: [{ where: {} }],
    ResourceAction: [{ where: {} }],
  },
}
