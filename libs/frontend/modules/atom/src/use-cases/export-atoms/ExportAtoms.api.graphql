# We return a base64 string for frontend to download the file
# https://stackoverflow.com/questions/54983080/return-file-from-graphql-resolve
query ExportAtoms($input: GetAtomsInput) {
  exportAtoms(input: $input) {
    payload
  }
}
