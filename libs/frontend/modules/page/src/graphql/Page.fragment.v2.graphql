fragment Page on Page {
  id
  name
  rootElement {
    id
    name
  }
}

fragment PageBase on Page {
  id
  name
  rootElement {
    id
    name
  }
  # Elements are expensive to get, don't query it every time
}

fragment PageFull on Page {
  ...PageBase
  # elementGraph {
  #   ...ElementGraph
  # }
}
