/**
 * These are generated from hasura GraphQL, enums are generated from tables
 *
 * Use this as a mapping of Hasura naming convention to our own, this gives us a central place to map to our own domain names
 */

/* eslint-disable camelcase */
import { Atom_Type_Enum, Lambda, Value_Type_Enum } from '@codelab/hasura'

export { Atom_Type_Enum as AtomType }
export { Value_Type_Enum as ValueType }

export type { Lambda }
