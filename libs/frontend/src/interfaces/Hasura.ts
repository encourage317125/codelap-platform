/**
 * These are generated from hasura GraphQL, enums are generated from tables
 *
 * Use this as a mapping of Hausra naming convention to our own, this gives us a central place to map to our own domain names
 */

/* eslint-disable camelcase */
import {
  Atom_Types_Enum,
  Lambda,
} from '../../../generated/src/graphql-client-hasura.generated'

export { Atom_Types_Enum as AtomType }

export type { Lambda }
