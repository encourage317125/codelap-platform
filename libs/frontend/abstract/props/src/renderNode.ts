import {
  ComponentFragment,
  ElementFragment,
  // ElementVertex,
} from '@codelab/shared/codegen/graphql'

export type RenderNode = ElementFragment | ComponentFragment

/**
 * Or should we use this? {@link ElementVertex}
 */
// export type RenderNode = ElementVertex
