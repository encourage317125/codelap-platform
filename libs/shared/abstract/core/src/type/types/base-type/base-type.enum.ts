/**
 * Used for seeding the base types and accessing them later
 */
export enum BaseTypeKind {
  Lambda = 'Lambda',
  Component = 'Component',
  Element = 'Element',
  ChildElement = 'Child Element',
  DescendantElement = 'Descendant Element',
  String = 'String',
  Boolean = 'Boolean',
  Float = 'Float',
  Integer = 'Integer',
  RenderProps = 'RenderProps',
  ReactNode = 'ReactNode',
}
