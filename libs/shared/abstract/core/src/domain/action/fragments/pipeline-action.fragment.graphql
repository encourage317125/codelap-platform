fragment PipelineAction on PipelineAction {
  ...ActionBase
  actions {
    ... on CustomAction {
      id
    }
    ... on ResourceAction {
      id
    }
    ... on PipelineAction {
      id
    }
  }
  # Connection causes issues #1779
  #  actionsConnection {
  #    edges {
  #      orders
  #      node {
  #        ... on CustomAction {
  #          id
  #        }
  #        ... on ResourceAction {
  #          id
  #        }
  #        ... on PipelineAction {
  #          id
  #        }
  #      }
  #    }
  #  }
}
