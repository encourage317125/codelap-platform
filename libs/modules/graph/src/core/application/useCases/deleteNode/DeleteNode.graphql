mutation DeleteNode($input: DeleteNodeInput!) {
  deleteNode(input: $input) {
    label
    vertices {
      id
      type
      props
    }
    edges {
      source
      target
      order
      props
    }
  }
}
