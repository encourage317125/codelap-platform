describe('Interface', () => {
  it('can create an interface', () => {
    /**
     * Let's model after the Antd Design button props
     */
    // (1) Create types
    // (1) Create fields
    // (2) Create an interface
  })
})
