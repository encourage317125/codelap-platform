import { Button } from 'antd'
import React from 'react'

const GraphPage = () => {
  // const createVertex = mutate(getApolloClient(), {
  //   mutation:
  // })

  return (
    <>
      <Button onClick={() => null}>Create Vertex</Button>
    </>
  )
}

export default GraphPage
