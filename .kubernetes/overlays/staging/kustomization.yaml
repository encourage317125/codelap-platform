apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

# resources:
#   - deployment.yaml
patchesStrategicMerge:
  - deployment.yaml

namespace: codelab
namePrefix: staging-
# configMapGenerator:
# - name: env-config
#   files:
#   - config.env
